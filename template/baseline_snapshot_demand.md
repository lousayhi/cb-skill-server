# 评测集基线快照生成需求单
## 1. 基线版本基础信息
基线版本号：
基线迭代日期：
基线覆盖范围：特征版本 / 规则包版本 / 模型版本 / 分流策略版本
基线描述：

## 2. 评测集数据源
均衡评测集文件/数据表：
样本唯一标识字段：sample_id
评测集总样本量：

## 3. 五层快照采集配置
### ① 原始样本层（全量采集）
需要留存原始业务字段清单：

### ② 特征层快照
全部衍生特征清单：
是否标记特征缺失/极值异常：是/否

### ③ 规则引擎层快照
风控规则ID&名称完整清单：
是否记录未命中规则（仅命中/全部规则）：仅命中

### ④ 模型层快照
子模型列表、融合模型名称：
是否存储分数分箱结果：是/否

### ⑤ 决策分流层快照
分流通道列表、审批策略、拒绝码枚举：

## 4. 快照存储配置
数据库类型：MySQL5.7 / MySQL8.0
快照表自定义名称：
单批次插入分片条数：1000 / 5000
是否创建联合唯一索引(version+sample_id)：是

## 5. 附加交付需求
1. 输出快照建表完整SQL
2. 输出分片批量入库INSERT脚本
3. 输出入库后快照完整性校验SQL
4. 提供多基线版本对比查询示例语句

## 6. 特殊约束
是否过滤脏数据样本不生成快照：是/否
是否记录每条样本特征/模型推理耗时：是/否