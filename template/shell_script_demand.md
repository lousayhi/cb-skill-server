# Shell运维测试脚本生成需求单
## 1. 基础环境信息
- 系统发行版：CentOS7/CentOS8/Ubuntu20.04/Ubuntu22.04
- 是否使用Docker容器：是/否
- 日志保存路径：
- 磁盘告警阈值(%)：
- 脚本超时全局时间：
- 接口请求重试次数：

## 2. 需要生成的脚本类型（可多选）
□ Linux环境校验脚本
□ 容器日志抓取排查脚本
□ 服务启停状态检测脚本
□ 批量接口健康巡检脚本

## 3. 环境校验配置（选填，生成环境脚本时填写）
- 需要检测端口列表：
- 关键检测目录：
- 需要连通测试的远程地址:端口：
- 检测进程名称：

## 4. 容器相关配置（选填，生成容器日志脚本时填写）
- 容器名称/容器名前缀：
- 日志抓取时间范围：近5m / 1h / 自定义
- 需要过滤的错误关键词：Error,Exception,Timeout
- 是否输出日志到本地文件：是/否

## 5. 服务检测配置（选填，生成服务检测脚本时填写）
检测模式：systemd服务 / 端口监听 / 进程名
待检测服务清单：
1. 服务名/端口/进程标识

## 6. 接口巡检配置（选填，生成接口巡检脚本时填写）
基础域名：
统一请求头(鉴权Token等)：
请求方式：GET/POST
批量接口清单：
1. /api/health 正常响应code=0
2. /api/user/status 正常响应code=0
响应校验关键字：
允许最大响应耗时(ms)：

## 7. 自定义扩展要求
1. 是否需要定时任务crontab配置示例
2. 是否需要异常发送简单告警输出（文本打印）
3. 其他自定义检测逻辑