# 性能瓶颈分析输入需求单
## 1. 基础压测指标
压测接口路径：
并发用户数：
稳定TPS：
平均响应时间RT(ms)：
P95/P99耗时(ms)：
报错率：
压测时长：

## 2. 服务器资源监控数据
CPU平均使用率：
CPU峰值使用率：
内存占用率：
GC频率（YGC/FGC）：
磁盘iowait均值：
磁盘读写峰值：

## 3. 数据库相关信息
慢日志核心内容（复制关键慢SQL）：
单表数据量级：
是否存在分页/批量更新场景：
锁等待超时次数：
explain执行结果关键信息：

## 4. 接口链路信息
接口内部调用逻辑：串行/并行
下游依赖服务数量：
是否存在循环查询数据库：
单次请求报文大小：

## 5. 已观测异常现象
并发升高后TPS下降 / RT抖动 / 偶发超时 / 数据库阻塞等

## 6. 预期优化目标
目标TPS：
目标P99响应时间(ms)：
允许报错率上限：