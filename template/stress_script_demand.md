# JMeter/Locust 压测脚本生成需求单
## 1. 基础压测引擎选择
输出脚本类型：JMeter JMX / Locust Python / 双引擎全部输出
压测场景：基准压测 / 梯度加压 / 长稳疲劳压测 / 混合业务链路压测

## 2. 服务基础信息
压测域名base_url：
接口统一超时时间(ms)：
鉴权方式：无 / Bearer Token / 登录接口提取token
全局固定请求头：

## 3. 并发调度配置
- 初始并发用户数：
- 最大并发数(梯度加压填写)：
- 每秒新增用户步长：
- 单用户循环间隔(ms)：
- 总压测时长(分钟)：
- 目标QPS吞吐量上限：

## 4. 压测业务接口链路（多接口混合填写流量权重）
格式：权重 Method Path 请求体/参数 校验规则
示例：
10 GET /api/user/list page=1,size=10 预期code=0，rt<200ms
5 POST /api/order/submit {"goodsId":1} 预期code=0

## 5. 参数池配置
参数类型：手机号/用户ID/订单编号/分页参数
参数取值范围/批量数据示例：
读取模式：随机取 / 顺序循环取

## 6. 性能校验阈值
单接口最大允许响应时间ms：
业务异常码是否视为失败：是/否
HTTP 5xx/4xx请求是否中断统计：是/否

## 7. 专项扩展配置
JMeter专属：是否需要分布式压测配置、后端监控监听器
Locust专属：是否需要分布式多机器压测、自定义性能指标
其他自定义逻辑：接口请求失败自动重试次数